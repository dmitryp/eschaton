jQuery.get('/location/create?location[latitude]=' + existing_location.lat() + '&location[longitude]=' + existing_location.lng() + '', function(data) {
  map.openInfoWindow(existing_location, "<div id='info_window_content'>" + data + "</div>", {});
});