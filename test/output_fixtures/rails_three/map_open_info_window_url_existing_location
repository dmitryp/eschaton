jQuery.get('/location/create?location[longitude]=' + existing_location.lng() + '&location[latitude]=' + existing_location.lat() + '', function(data) {
  map.openInfoWindow(existing_location, "<div id='info_window_content'>" + data + "</div>", {});
});